import { ModeStub } from "@/components/next/shell/ModeStub";

export default function NextTriagePage() {
  return (
    <ModeStub
      title="Triage"
      body="Alerts and the investigation queue as working queues. Generator-authored Cases will be shown separately here and never presented as equivalent to an analyst's own investigation."
      phase="ARGUS_PLAN.md — Phase 9"
    />
  );
}
